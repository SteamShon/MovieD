<div class="alert alert-block">
<button type="button" class="close" data-dismiss="alert">×</button>
<h4>Guiude!</h4>
<ol>
  <li>Search 5~10 movies by their name on left search form</li>
  <li>Rate movies on search result</li>
  <li>Once you rated more than 5~10 movies, then click analyze to discover</li>
  <li>To see movie poster, click film icon(note: this is just top search result image from movie.naver.com</li>
</ol>
</div>

<div class="row">
  <div class="span2"><%= link_to "<< Back", user_path(current_user),  { class: "pager" } %></div>
  <div class="span10"><h1><%= Tag.find(@tag_id).title %></h1></div>
</div>

  <div class="row-fluid">

    <div class="span6">
      <%= render "search" %>
      <h2><%= params[:query] ? "Search result" : "Top rank movies" %></h2>
      <% for movie in @movies %>
        <div class="row">
          <div class="span5" id="<%= movie.id %>">
            <%= link_to movie.title, naver_movie_url(movie.title), target: "_blank" %><br />
            <%= link_to '<i class="icon-film"></i>'.html_safe, 
                movie, { method: :get, remote: true } %>
          </div>
          
          <div class="span1">
            <%= render partial: "/rates/form", locals: { movie: movie } %>
          </div>
        </div>
      <% end %>

      <%= will_paginate @movies %>
      <br />
    </div>

    <div class="span6">
      <div class="user_rated">
        <%= render partial: "/rates/user_rated" %>
      </div>

      <div class="recs">
      </div>      
    </div>
  </div>
</div>
